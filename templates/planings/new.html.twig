{% extends 'base.html.twig' %}

{% block title %}New Planing{% endblock %}

{% block body %}
    <h1>Create new Planing</h1>
<div style="
    margin-left: 29%;
    margin-top: 20%;
">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_planings_index') }}">back to list</a>
    </div>
{% endblock %}
