{% extends 'base.html.twig' %}

{% block section %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
        </tbody>
    </table>

<button  class="btn btn-outline-secondary"><a style="color: black" href="{{ path('app_categorie_index') }}">back</a>

    <button class="btn btn-primary"> <a style="color: white" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
