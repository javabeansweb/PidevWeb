{% extends 'base.html.twig' %}





{% block section %}
    <h1 style="margin-left: 350px">Users </h1>
    <table class="table"    style="max-width: 800px ; margin: auto">
        <thead>
        <tr>

            <th>Email</th>
            <th>Nom et prénom</th>
            <th>Date de Naissance</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>


                <td>{{ user.username }}</td>
                <td>{{ user.fullname }}</td>
                <td>{{ user.naissance ? user.naissance|date('Y-m-d') : '' }}</td>


                <td>


                    <button class="bg-danger-light" ><a href="{{path('deleteclient', {'id': user.id}) }}"method="post" >delete</a></button>


                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
