{% extends 'base.html.twig' %}


{% block body%}
<div class="body" style="margin: 150px 0 0 500px">
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_index') }}">back to list</a>

    {{ include('user/_delete_form.html.twig') }}
</div>
{% endblock %}
